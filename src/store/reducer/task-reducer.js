const initialState = {
    tasks: [],
    selectedTask: {},
};

export const tasksReducer = (state = initialState, action) => {
    switch (action.type) {
        default: 
            return state;
    };
};
